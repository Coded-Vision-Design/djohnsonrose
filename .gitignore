# OS files
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/
*.sublime-project
*.sublime-workspace

# Environment files
.env
.env.local
.env.*.local
.env.production
.env.development

# Local Claude / agent config — kept on disk, never pushed.
CLAUDE.md
.claude/
.githooks/

# Logs
*.log

# Original camera-roll uploads — committed as transcoded WebM/MP4 instead.
*.MOV
*.mov

# Build outputs
dist/
build/
.cache/
.tmp/

# Dependencies
node_modules/
v2/node_modules/

# Test artefacts
v2/test-results/
v2/playwright-report/
coverage/

# Private files (CV PDF stays on the server only — the binary printable).
# cv.php (HTML rendering for the Word app) lives in git because the
# Word app's content is already public on the live site; treating it as
# private just makes maintenance impossible.
data/cv.pdf
data/weather_cache.json

# References plaintext only — only the encrypted envelope is committed.
data/references.json
data/references.sample.json

# DNS zone exports (kept locally for reference, not for the public repo).
*.co.uk.txt
*.com.txt
*.zone

# Vercel
.vercel/

# PHP / Composer
vendor/
composer.phar
*.cache
*.bak

# WordPress
wp-content/uploads/
wp-content/upgrade/
wp-config-local.php
